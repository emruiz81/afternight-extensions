# Packaging Notes

Cosmic Clarity is published as a single pure-Python adapter asset for all current AfterNight runtime targets.

The package intentionally excludes upstream Cosmic Clarity executables, models, and runtime folders. The extension UI asks users to configure the folder containing those tools after installation.
