[tool.ruff]
target-version = "py314"
line-length = 120
extend-exclude = [
  "dist",
  "dist-public-check",
]

[tool.ruff.lint]
select = [
  "E4",
  "E7",
  "E9",
  "F",
]

[tool.ruff.format]
line-ending = "lf"
